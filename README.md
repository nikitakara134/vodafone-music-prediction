# Vodafone Music Prediction: Propensity to Purchase Model

## Опис Бізнес-Задачі (Business Understanding)

**Мета проєкту:**
Розробка моделі машинного навчання для прогнозування ймовірності встановлення та оплати додатку **Vodafone Music** існуючими абонентами.

**Бізнес-цінність:**
Модель дозволить оптимізувати маркетинговий бюджет шляхом переходу від масових розсилок до **таргетованого промотування**.
* **Зменшення витрат:** Ми не витрачаємо ресурси на комунікацію з клієнтами, яким послуга не цікава.
* **Зменшення відтоку (Churn prevention):** Ми не дратуємо лояльних клієнтів зайвим спамом.
* **Збільшення конверсії:** Пропозиція надходить тим, хто з високою ймовірністю готовий платити.

---

## Постановка Задачі ML

**Тип задачі:** Binary Classification (Бінарна класифікація).

**Цільова змінна (Target):**
* `1` (Positive): Абонент встановив додаток та оплатив перший місяць користування.
* `0` (Negative): Абонент не встановив додаток або не оплатив його.

**Метрика якості:**
**ROC-AUC**.
Ця метрика обрана, оскільки нам важлива **якість ранжування** (ranking quality) — модель повинна присвоювати вищі ймовірності (score) саме тим клієнтам, які реально зацікавлені в покупці. Також ROC-AUC стійка до сильного дисбалансу класів, який присутній у наших даних.

**Ціна помилки (Cost of Error):**
1.  **False Positive (FP):** Ми передбачили, що клієнт купить, але він не купив.
    * *Наслідок:* Марні витрати на SMS/дзвінок, ризик роздратування клієнта.
2.  **False Negative (FN):** Ми передбачили, що клієнт не купить, а він був готовий.
    * *Наслідок:* Втрачена вигода (Revenue Loss). Це більш критична помилка, якщо вартість залучення клієнта (CAC) значно менша за прибуток від нього (LTV).

---

## Аналіз Даних (Data Understanding & Initial EDA)

**Загальна інформація:**
* **Розмір датасету:** 70,000 записів (абонентів).
* **Кількість ознак (Features):** 461 змінна.

**Категорії даних:**
Дані включають поведінкові фактори та статистику користування послугами:
* *Device characteristics* (тип пристрою, ОС).
* *User activity* (баланс, дні активності).
* *Voice & SMS stats* (дзвінки, витрати, роумінг).
* *Internet usage* (трафік, категорії відвідуваних сайтів).
* *Previous Campaigns* (реакція на минулі акції).

### Ключові інсайти Sprint 1:

1.  **Дисбаланс класів (Class Imbalance):**
    Спостерігається значний перекіс у бік негативного класу:
    * Target `0`: **92.4%**
    * Target `1`: **7.6%**
    * *Висновок:* Необхідно використовувати методи балансування (class weights) або відповідні метрики (ROC-AUC, Precision-Recall) при моделюванні.

2.  **Пропущені значення (Missing Values):**
    * Близько **449 фіч** мають пропуски.
    * Деякі змінні (наприклад, `count_url_category_13`, `vol_app_3`) пропущені у >98% випадків.
    * *Стратегія обробки:* Числові пропуски заповнені медіаною, категоріальні - модою. Константні колонки видалені.

3.  **Результат очищення:**
    Після Baseline-обробки (видалення дублікатів та константних значень) розмірність даних складає: **(70000, 442)**.

### Sprint 2: Advanced EDA, Feature Engineering & Baseline Models

**Ціль спринту:** Знайти найважливіші патерни в даних, зменшити розмірність датасету та побудувати перші працюючі ML-моделі (Baselines) для порівняння.

**Що було зроблено:**

**1. Глибокий аналіз даних (Advanced EDA) та усунення Data Leakage:**
* Розраховано **Pearson Correlation** та **Mutual Information (MI)** для оцінки зв'язку кожної фічі з цільовою змінною `target`.
*  **Критична знахідка (Data Leakage):** Аналіз показав, що колонка `id` (ідентифікатор абонента) мала аномально високу кореляцію з таргетом (найвищу серед усіх 442 ознак). Щоб модель не запам'ятовувала специфіку вивантаження бази даних (артефакти) і могла працювати на нових клієнтах, ознаку `id` було примусово **видалено**.

**2. Відбір ознак (Feature Selection):**
* Після усунення `id` проведено фільтрацію ознак за допомогою оцінки *Mutual Information*.
* Усі "шумові" фічі (зі значенням MI < 0.001) були відкинуті. 
* **Результат:** Простір ознак успішно зменшено з **441 до 218** найінформативніших змінних. Це кардинально зменшує ризик перенавчання (overfitting) та прискорює тренування моделей.

**3. Підготовка даних (Data Preparation):**
* Датасет розділено на навчальну та валідаційну вибірки у пропорції **80/20**. 
* Застосовано параметр `stratify=y`, щоб зберегти оригінальний дисбаланс класів (92% на 8%) в обох підвибірках.
* Усі числові ознаки відмасштабовано за допомогою `StandardScaler` (необхідно для коректної роботи лінійних моделей та kNN).

**4. Побудова базових моделей (Baseline Modeling):**
Навчено три алгоритми різної природи для встановлення базового рівня (baseline). Головний фокус — правильна обробка дисбалансу класів:
* **Logistic Regression:** Базова лінійна модель (використано `class_weight='balanced'`).
* **Random Forest:** Ансамбль дерев (використано обмеження `max_depth=10` та `class_weight='balanced'`).
* **LightGBM:** Градієнтний бустинг (використано `scale_pos_weight=11.5` — розраховано як співвідношення негативного класу до позитивного: ~92.4 / 7.6).

**Ключові результати спринту:**
* Модель **LightGBM** стала беззаперечним лідером, показавши **ROC-AUC = 0.816** (на валідаційній вибірці).
* Random Forest показав ROC-AUC = 0.796, а Logistic Regression = 0.788.
* Метрика **F1-score** у всіх моделей наразі низька (~0.33), оскільки алгоритми використовують стандартний поріг відсічення ймовірності (0.5), який не підходить для задач із сильним дисбалансом. Оптимізація цього порогу стане задачею наступного спринту.
---

## 📂 Структура Репозиторію

```text
├── data/
│   ├── raw/                 # Вихідні дані (train_music.csv)
│   └── processed/           # Очищені дані (train_music_cleaned.csv)
├── notebooks/
│   └── 01_EDA_and_Cleaning.ipynb  # Аналіз, візуалізація та очищення даних
├── src/                     # Допоміжні скрипти
└── README.md                # Опис проєкту